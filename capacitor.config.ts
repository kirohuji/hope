import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lourd.ionic.starter',
  appName: 'myapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
